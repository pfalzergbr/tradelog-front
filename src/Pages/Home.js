import React from 'react';
import {useLocation, useParams, useHistory, useRouteMatch} from 'react-router-dom'

const Home = (props) => {
    console.log(props)
    return <div>Home!!</div>;
};

export default Home;
